"""SincroGit — automatic synchronization with robust Git versioning.

Phase 1: watcher + snapshot (amend) + periodic sealing with a fallback message.
Phase 2: AI messages, push and pull. Phase 4: system tray UI. See DESIGN.md.
"""

__version__ = "0.1.0"
